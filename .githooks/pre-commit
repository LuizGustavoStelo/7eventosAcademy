#!/usr/bin/env sh

echo "[pre-commit] Verificando encoding (BOM/mojibake)..."
node scripts/check-encoding.mjs --staged
status=$?

if [ $status -ne 0 ]; then
  echo "[pre-commit] Commit bloqueado por erro de encoding."
  exit $status
fi

exit 0
